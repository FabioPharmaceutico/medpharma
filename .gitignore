node_modules
.next
.env
.env.*
!.env.example
*.log
dist
prisma/dev.db
prisma/*.db

# Saídas de OCR do bulário (obra protegida) — não publicar
tools/bulario/*.json

# CSVs brutos baixados de fontes oficiais (não versionar o arquivo grande)
*.csv

.vercel
